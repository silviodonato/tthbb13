[global]
variable markers   = @
task        = UserTask           ; Job uses user written scripts
backend     = local              ; Send to local batch system
workdir = $CMSSW_BASE/src/TTH/MEAnalysis/gc/work.count

[local]
sandbox path = $CMSSW_BASE/src/TTH/MEAnalysis/gc/work.count/sandbox

[jobs]
wall time = 1:00:00      ; Requested wall time in format hh[:mm[:ss]]

[UserTask]
executable  = count.sh
dataset splitter = FileBoundarySplitter
files per job = 100
input files = common.sh env.sh
dataset =
    datasets/VHBBHeppyV22pre_tthbbV10pre_pilot1/TT_TuneEE5C_13TeV-powheg-herwigpp.txt

[storage]
scratch space used = 5000
scratch space left = 1000
